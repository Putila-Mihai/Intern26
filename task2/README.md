Make a domain model for a store inventory along with adding the basic functionalities it should have:

Create an item with properties such as name, details, quantity in stock, availability, price
An Item can belong to a category
A shopping cart model which can have many items with total price.
An order item created after user checkout. It will have order id, date purchased, items
Assign item to category
Print list of items given a category
Add item stock to inventory
Create an shopping cart containing items
Add items to cart (You can’t add items if the quantity is out of stock)
Print shopping cart items with the price and total
Checkout and create order, also print order detail