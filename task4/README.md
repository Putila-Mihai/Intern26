Create a generic class Pair that can hold two objects of the same type. The class should be able to store any type of objects (e.g., Integer, String, Double) and provide methods to access and set these objects. For example:

* getFirst(): Returns the first object of the pair.
* getSecond(): Returns the second object of the pair.
* setFirst(T first): Sets the first object of the pair.
* setSecond(T second): Sets the second object of the pair.
* toString(): Override the toString method to return a string representation of the pair in the format "Pair(first, second)".

Try using your own custom classes, some bounded types generics, experiment a little.
In main add examples of usage, instantiate the Pair with different data type.